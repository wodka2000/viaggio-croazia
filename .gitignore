node_modules/
dist/

# Environment variables — MAI committare
.env
.env.local
.env.*.local

# Config con API key — usa config.template.js come riferimento
src/config.js

# Documenti personali / prenotazioni — MAI committare (repo pubblico)
# Nessuna eccezione: i biglietti contengono nomi, date di nascita e numeri di
# documento (anche dei bambini). Committarli qui li rende scaricabili da
# chiunque, sia dal repo sia dal sito. Si allegano dalla webapp e restano sul
# dispositivo (utils/attachments.js) — cosi funzionano pure senza rete.
*.pdf

# Utility personale per rileggere le conversazioni: non c'entra con l'app, e
# la pagina che genera contiene il testo integrale delle chat.
tools/esporta-conversazione.ps1

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.suo
*.user
